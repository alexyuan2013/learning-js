{
  "name": "voting_app",
  "version": "1.0.0",
  "description": "a demo for basic react usage",
  "main": "index.js",
  "scripts": {
    "go": "open http://localhost:3000; npm run server",
    "e2e": "nightwatch",
    "test": "./node_modules/.bin/concurrently -k 'npm run server' 'npm run e2e'",
    "start": "npm run server",
    "server": "http-server . -p 3000"
  },
  "keywords": [
    "react"
  ],
  "author": "alex",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.9.0"
  },
  "devDependencies": {
    "concurrently": "^3.4.0"
  }
}
